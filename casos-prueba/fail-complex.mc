main() {
    var int a, b;
    const int valor = 5, incremento = 3;

    read(a, c);               
    print("Iniciando...");

    resultado = a + b;       

    if (a )               
    {
        a = a + incremento;
    } else {
        valor = valor + 1;    
    }

    /* testing fail in comments              
}

casi
*/}